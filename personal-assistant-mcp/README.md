# 🤖 Personal Assistant MCP Server

개인 비서 MCP 서버 - 메모, 할 일, 일정, 북마크를 관리하는 MCP 서버입니다.

## 📦 설치

```bash
cd personal-assistant-mcp
npm install
npm run build
```

## 🔧 Cursor 설정

`~/.cursor/mcp.json` 파일에 다음 내용을 추가하세요:

```json
{
  "mcpServers": {
    "personal-assistant": {
      "command": "node",
      "args": ["/Users/jangjunho/dev/myMCP/personal-assistant-mcp/dist/index.js"]
    }
  }
}
```

설정 후 Cursor를 재시작하면 MCP 서버가 연결됩니다.

## 🛠️ 제공 도구 (Tools)

### 📝 메모 관리
| 도구 | 설명 |
|------|------|
| `create_memo` | 새 메모 생성 |
| `list_memos` | 메모 목록 조회 (검색/태그 필터링) |
| `get_memo` | 특정 메모 상세 조회 |
| `update_memo` | 메모 수정 |
| `delete_memo` | 메모 삭제 |

### ✅ 할 일 관리
| 도구 | 설명 |
|------|------|
| `create_todo` | 새 할 일 생성 |
| `list_todos` | 할 일 목록 조회 (완료여부/우선순위/태그 필터링) |
| `get_todo` | 특정 할 일 상세 조회 |
| `update_todo` | 할 일 수정 (완료 처리 포함) |
| `delete_todo` | 할 일 삭제 |

### 📅 일정 관리
| 도구 | 설명 |
|------|------|
| `create_schedule` | 새 일정 생성 |
| `list_schedules` | 일정 목록 조회 (날짜 범위/태그 필터링) |
| `get_schedule` | 특정 일정 상세 조회 |
| `update_schedule` | 일정 수정 |
| `delete_schedule` | 일정 삭제 |

### 🔖 북마크 관리
| 도구 | 설명 |
|------|------|
| `create_bookmark` | 새 북마크 생성 |
| `list_bookmarks` | 북마크 목록 조회 (검색/태그 필터링) |
| `get_bookmark` | 특정 북마크 상세 조회 |
| `update_bookmark` | 북마크 수정 |
| `delete_bookmark` | 북마크 삭제 |

### 🔍 통합 기능
| 도구 | 설명 |
|------|------|
| `search_all` | 모든 데이터에서 통합 검색 |
| `get_today_summary` | 오늘의 요약 (일정, 마감 할 일, 최근 메모) |

## 📚 제공 리소스 (Resources)

| URI | 설명 |
|-----|------|
| `assistant://summary/today` | 오늘의 요약 정보 |
| `assistant://todos/incomplete` | 미완료 할 일 목록 |
| `assistant://schedules/week` | 이번 주 일정 |

## 💾 데이터 저장

모든 데이터는 SQLite 데이터베이스에 저장됩니다:
- 위치: `personal-assistant-mcp/data/assistant.db`

## 📖 사용 예시

Cursor에서 AI와 대화하며 사용:

```
"오늘 할 일 추가해줘: 보고서 작성, 우선순위 높음"
"내 메모 목록 보여줘"
"오늘 일정이 뭐야?"
"이 URL 북마크해줘: https://example.com"
"오늘 요약해줘"
```

## 🎓 MCP 개념 학습

이 프로젝트에서 배울 수 있는 MCP 개념:

1. **Server**: MCP 서버 기본 구조
2. **Tools**: 실행 가능한 도구 정의 및 구현
3. **Resources**: 읽기 전용 데이터 리소스 제공
4. **Transport**: stdio를 통한 통신
5. **Schema**: zod를 이용한 입력 검증

## 📁 프로젝트 구조

```
personal-assistant-mcp/
├── src/
│   ├── index.ts          # MCP 서버 메인 파일
│   ├── database.ts       # SQLite 데이터베이스 설정
│   └── tools/
│       ├── memo.ts       # 메모 관련 기능
│       ├── todo.ts       # 할 일 관련 기능
│       ├── schedule.ts   # 일정 관련 기능
│       └── bookmark.ts   # 북마크 관련 기능
├── data/                 # SQLite DB 파일 (자동 생성)
├── dist/                 # 빌드 결과물
├── package.json
├── tsconfig.json
└── README.md
```

