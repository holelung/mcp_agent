# 🏗️ Architecture

Personal Assistant MCP 프로젝트의 전체 아키텍처 설명입니다.

---

## 📊 시스템 개요

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              사용자 인터페이스                                │
├───────────────────────────────────┬─────────────────────────────────────────┤
│         Cursor IDE (AI)           │           Web Dashboard                 │
│   ┌─────────────────────────┐     │     ┌─────────────────────────┐         │
│   │   Claude AI Assistant   │     │     │   Vue.js + Vite App     │         │
│   └───────────┬─────────────┘     │     └───────────┬─────────────┘         │
│               │                   │                 │                       │
│               │ JSON-RPC 2.0      │                 │ HTTP REST             │
│               │ (stdio)           │                 │                       │
└───────────────┼───────────────────┴─────────────────┼───────────────────────┘
                │                                     │
┌───────────────┼───────────────────────────────────────────────────────────────┐
│               ▼                     백엔드 서비스    ▼                         │
│   ┌─────────────────────────┐           ┌─────────────────────────┐           │
│   │    MCP Server           │           │    REST API Server      │           │
│   │    (Node.js/TS)         │           │    (Express.js)         │           │
│   │    Port: stdio          │           │    Port: 3001           │           │
│   └───────────┬─────────────┘           └───────────┬─────────────┘           │
│               │                                     │                         │
│               └──────────────┬──────────────────────┘                         │
│                              │                                                │
│                              ▼                                                │
│                  ┌─────────────────────────┐                                  │
│                  │     PostgreSQL          │                                  │
│                  │     (Docker)            │                                  │
│                  │     Port: 5432          │                                  │
│                  └─────────────────────────┘                                  │
└───────────────────────────────────────────────────────────────────────────────┘
```

---

## 📁 프로젝트 구조

```
myMCP/
├── 📄 README.md                    # 프로젝트 개요
├── 📄 docker-compose.yml           # 전체 서비스 오케스트레이션
├── 📂 docs/
│   ├── 📄 CHANGELOG.md             # 변경 내역
│   └── 📄 ARCHITECTURE.md          # 아키텍처 문서 (현재 문서)
│
├── 📂 personal-assistant-mcp/      # MCP 서버
│   ├── 📄 package.json
│   ├── 📄 tsconfig.json
│   ├── 📄 docker-compose.yml       # DB 전용 compose
│   ├── 📄 init.sql                 # DB 스키마 초기화
│   └── 📂 src/
│       ├── 📄 index.ts             # MCP 서버 진입점
│       ├── 📄 database.ts          # DB 연결 관리
│       └── 📂 tools/               # MCP 도구들
│           ├── 📄 memo.ts
│           ├── 📄 todo.ts
│           ├── 📄 schedule.ts
│           └── 📄 bookmark.ts
│
├── 📂 personal-assistant-backend/  # REST API 서버
│   ├── 📄 package.json
│   ├── 📄 tsconfig.json
│   ├── 📄 Dockerfile
│   └── 📂 src/
│       └── 📄 server.ts            # Express 서버
│
└── 📂 personal-assistant-dashboard/ # 웹 대시보드
    ├── 📄 package.json
    ├── 📄 vite.config.ts
    ├── 📄 tailwind.config.js
    ├── 📄 Dockerfile
    ├── 📄 nginx.conf
    └── 📂 src/
        ├── 📄 main.ts              # Vue 앱 진입점
        ├── 📄 App.vue              # 루트 컴포넌트
        ├── 📄 style.css            # 전역 스타일
        ├── 📂 api/
        │   └── 📄 index.ts         # API 클라이언트
        ├── 📂 components/
        │   └── 📄 Sidebar.vue      # 사이드바
        └── 📂 views/
            ├── 📄 Dashboard.vue    # 대시보드
            ├── 📄 Memos.vue        # 메모 관리
            ├── 📄 Todos.vue        # 할일 관리
            ├── 📄 Schedules.vue    # 일정 관리
            └── 📄 Bookmarks.vue    # 북마크 관리
```

---

## 🔧 컴포넌트 상세

### 1. MCP Server (`personal-assistant-mcp`)

**역할:** Cursor IDE의 AI 어시스턴트와 통신하여 데이터를 관리하는 MCP 서버

**기술 스택:**
- Node.js + TypeScript
- `@modelcontextprotocol/sdk` - MCP 프로토콜 구현
- `pg` - PostgreSQL 클라이언트
- JSON-RPC 2.0 over stdio

**제공 도구 (Tools):**

| 도구명 | 설명 |
|--------|------|
| `create_memo` | 새 메모 생성 |
| `list_memos` | 메모 목록 조회 (검색/태그 필터) |
| `get_memo` | 특정 메모 조회 |
| `update_memo` | 메모 수정 |
| `delete_memo` | 메모 삭제 |
| `create_todo` | 새 할일 생성 |
| `list_todos` | 할일 목록 조회 |
| `get_todo` | 특정 할일 조회 |
| `update_todo` | 할일 수정/완료 처리 |
| `delete_todo` | 할일 삭제 |
| `create_schedule` | 새 일정 생성 |
| `list_schedules` | 일정 목록 조회 |
| `get_schedule` | 특정 일정 조회 |
| `update_schedule` | 일정 수정 |
| `delete_schedule` | 일정 삭제 |
| `create_bookmark` | 새 북마크 생성 |
| `list_bookmarks` | 북마크 목록 조회 |
| `get_bookmark` | 특정 북마크 조회 |
| `update_bookmark` | 북마크 수정 |
| `delete_bookmark` | 북마크 삭제 |
| `search_all` | 전체 데이터 통합 검색 |
| `get_today_summary` | 오늘의 요약 정보 |

**MCP 설정 (Cursor):**
```json
{
  "mcpServers": {
    "personal-assistant": {
      "command": "node",
      "args": ["/path/to/personal-assistant-mcp/dist/index.js"]
    }
  }
}
```

---

### 2. REST API Server (`personal-assistant-backend`)

**역할:** 웹 대시보드에 데이터를 제공하는 REST API 서버

**기술 스택:**
- Node.js + TypeScript
- Express.js
- `pg` - PostgreSQL 클라이언트
- CORS 지원

**API 엔드포인트:**

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/summary` | 오늘 요약 정보 |
| GET | `/api/memos` | 메모 목록 |
| POST | `/api/memos` | 메모 생성 |
| PUT | `/api/memos/:id` | 메모 수정 |
| DELETE | `/api/memos/:id` | 메모 삭제 |
| GET | `/api/todos` | 할일 목록 |
| POST | `/api/todos` | 할일 생성 |
| PUT | `/api/todos/:id` | 할일 수정 |
| DELETE | `/api/todos/:id` | 할일 삭제 |
| GET | `/api/schedules` | 일정 목록 |
| POST | `/api/schedules` | 일정 생성 |
| PUT | `/api/schedules/:id` | 일정 수정 |
| DELETE | `/api/schedules/:id` | 일정 삭제 |
| GET | `/api/bookmarks` | 북마크 목록 |
| POST | `/api/bookmarks` | 북마크 생성 |
| PUT | `/api/bookmarks/:id` | 북마크 수정 |
| DELETE | `/api/bookmarks/:id` | 북마크 삭제 |

---

### 3. Web Dashboard (`personal-assistant-dashboard`)

**역할:** 데이터를 시각화하고 관리하는 웹 인터페이스

**기술 스택:**
- Vue.js 3 (Composition API)
- Vite (빌드 도구)
- TypeScript
- Tailwind CSS
- Nginx (프로덕션 서빙)

**주요 기능:**
- 📊 **대시보드**: 오늘 요약, 통계 카드, 일정 미리보기
- 📝 **메모**: CRUD, 검색, 태그 필터, 상세 모달
- ✅ **할일**: CRUD, 우선순위, 마감일, 완료 토글, 상세 모달
- 📅 **일정**: CRUD, 캘린더 뷰, 날짜별 필터
- 🔖 **북마크**: CRUD, 태그 필터, 외부 링크

**반응형 디자인:**
- 모바일: 햄버거 메뉴, 스택 레이아웃
- 태블릿: 2열 그리드
- 데스크톱: 사이드바 + 넓은 콘텐츠
- 대형 화면 (FHD/QHD/4K): 확장된 캘린더, 큰 폰트

---

### 4. PostgreSQL Database

**역할:** 모든 데이터의 영구 저장소

**스키마:**

```sql
-- 메모 테이블
CREATE TABLE memos (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    content TEXT NOT NULL,
    tags TEXT[] DEFAULT '{}',
    search_vector TSVECTOR,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 할일 테이블
CREATE TABLE todos (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    description TEXT DEFAULT '',
    completed BOOLEAN DEFAULT FALSE,
    priority VARCHAR(20) DEFAULT 'medium',
    due_date TIMESTAMP,
    tags TEXT[] DEFAULT '{}',
    search_vector TSVECTOR,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 일정 테이블
CREATE TABLE schedules (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    description TEXT DEFAULT '',
    start_time TIMESTAMP NOT NULL,
    end_time TIMESTAMP,
    location VARCHAR(255) DEFAULT '',
    tags TEXT[] DEFAULT '{}',
    search_vector TSVECTOR,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 북마크 테이블
CREATE TABLE bookmarks (
    id SERIAL PRIMARY KEY,
    url TEXT NOT NULL,
    title VARCHAR(255) NOT NULL,
    description TEXT DEFAULT '',
    tags TEXT[] DEFAULT '{}',
    search_vector TSVECTOR,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**인덱스:**
- `search_vector`에 GIN 인덱스 (전문 검색)
- `tags`에 GIN 인덱스 (배열 검색)

---

## 🔄 데이터 흐름

### MCP를 통한 데이터 흐름 (AI 어시스턴트)

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   사용자     │ --> │  Cursor AI   │ --> │  MCP Server  │ --> │  PostgreSQL  │
│  (자연어)    │     │  (Claude)    │     │  (Tools)     │     │  (DB)        │
└──────────────┘     └──────────────┘     └──────────────┘     └──────────────┘
        │                   │                     │                    │
        │  "메모 추가해줘"   │                     │                    │
        │─────────────────>│                     │                    │
        │                   │  create_memo()     │                    │
        │                   │────────────────────>│                    │
        │                   │                     │   INSERT INTO...   │
        │                   │                     │───────────────────>│
        │                   │                     │      OK            │
        │                   │                     │<───────────────────│
        │                   │    { id: 1, ... }  │                    │
        │                   │<────────────────────│                    │
        │  "메모가 생성됐어요" │                    │                    │
        │<──────────────────│                     │                    │
```

### REST API를 통한 데이터 흐름 (웹 대시보드)

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   브라우저   │ --> │   Nginx      │ --> │  Express     │ --> │  PostgreSQL  │
│  (Vue.js)    │     │  (Proxy)     │     │  (REST API)  │     │  (DB)        │
└──────────────┘     └──────────────┘     └──────────────┘     └──────────────┘
        │                   │                     │                    │
        │  GET /api/todos   │                     │                    │
        │──────────────────>│                     │                    │
        │                   │  GET /api/todos    │                    │
        │                   │────────────────────>│                    │
        │                   │                     │  SELECT * FROM...  │
        │                   │                     │───────────────────>│
        │                   │                     │    [rows]          │
        │                   │                     │<───────────────────│
        │                   │    [{ id: 1, ...}] │                    │
        │                   │<────────────────────│                    │
        │   JSON Response   │                     │                    │
        │<──────────────────│                     │                    │
```

---

## 🐳 Docker 구성

### 개발 환경

```yaml
# personal-assistant-mcp/docker-compose.yml
services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
```

### 프로덕션 환경

```yaml
# docker-compose.yml (루트)
services:
  db:
    image: postgres:16-alpine
    
  backend:
    build: ./personal-assistant-backend
    depends_on:
      - db
      
  dashboard:
    build: ./personal-assistant-dashboard
    ports:
      - "80:80"
    depends_on:
      - backend
```

---

## 🔐 환경 변수

### MCP Server / Backend

```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=personal_assistant
DB_USER=assistant
DB_PASSWORD=password
```

### Dashboard (개발)

```typescript
// vite.config.ts
server: {
  proxy: {
    '/api': 'http://localhost:3001'
  }
}
```

---

## 🚀 실행 방법

### 1. 데이터베이스 시작

```bash
cd personal-assistant-mcp
docker-compose up -d
```

### 2. MCP 서버 빌드

```bash
cd personal-assistant-mcp
npm install
npm run build
```

### 3. REST API 서버 시작

```bash
cd personal-assistant-backend
npm install
npm run dev
```

### 4. 대시보드 시작

```bash
cd personal-assistant-dashboard
npm install
npm run dev
```

### 5. Cursor MCP 설정

`~/.cursor/mcp.json`에 MCP 서버 등록

---

## 📈 확장 가능성

### 향후 개선 사항

1. **인증/권한 시스템**
   - JWT 기반 인증
   - 다중 사용자 지원

2. **알림 시스템**
   - 일정 알림
   - 마감일 리마인더

3. **동기화**
   - Google Calendar 연동
   - Notion 연동

4. **AI 기능 강화**
   - 자동 태그 추천
   - 일정 충돌 감지
   - 스마트 검색

5. **모바일 앱**
   - React Native / Flutter 앱
   - PWA 지원

---

## 📚 참고 자료

- [Model Context Protocol](https://modelcontextprotocol.io/)
- [Vue.js Documentation](https://vuejs.org/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [Tailwind CSS](https://tailwindcss.com/)
- [Express.js](https://expressjs.com/)

