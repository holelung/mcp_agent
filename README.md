# 🤖 Personal Assistant MCP

MCP(Model Context Protocol)를 활용한 개인 비서 시스템입니다. Cursor IDE와 연동하여 메모, 할 일, 일정, 북마크를 관리할 수 있습니다.

## 📁 프로젝트 구조

```
myMCP/
├── personal-assistant-mcp/       # MCP 서버 (Cursor IDE 연동)
├── personal-assistant-backend/   # REST API 서버
├── personal-assistant-dashboard/ # Vue.js 웹 대시보드
└── docker-compose.yml            # 통합 Docker Compose
```

## ✨ 주요 기능

### 📝 메모 관리
- 메모 생성, 조회, 수정, 삭제
- 태그 기반 분류
- 전문 검색 (Full-text search)

### ✅ 할 일 관리
- 할 일 생성 및 완료 처리
- 우선순위 설정 (높음/보통/낮음)
- 마감일 관리

### 📅 일정 관리
- 일정 생성 및 관리
- 시작/종료 시간 설정
- 장소 정보 추가

### 🔖 북마크 관리
- URL 북마크 저장
- 태그 기반 분류
- 설명 추가

### 🔍 통합 검색
- 모든 데이터 통합 검색
- 오늘의 요약 정보 조회

## 🛠️ 기술 스택

| 구성요소 | 기술 |
|---------|------|
| MCP 서버 | Node.js, TypeScript, @modelcontextprotocol/sdk |
| 백엔드 API | Express.js, TypeScript |
| 프론트엔드 | Vue 3, Vite, Tailwind CSS |
| 데이터베이스 | PostgreSQL 16 |
| 컨테이너 | Docker, Docker Compose |

## 🚀 시작하기

### 사전 요구사항

- Node.js 20+
- Docker & Docker Compose
- Cursor IDE (MCP 클라이언트)

### 1. PostgreSQL 실행

```bash
# 프로젝트 루트에서
docker-compose up -d postgres
```

### 2. MCP 서버 빌드

```bash
cd personal-assistant-mcp
npm install
npm run build
```

### 3. Cursor IDE 설정

`~/.cursor/mcp.json` 파일에 다음 내용 추가:

```json
{
  "mcpServers": {
    "personal-assistant": {
      "command": "node",
      "args": ["/path/to/myMCP/personal-assistant-mcp/dist/index.js"]
    }
  }
}
```

### 4. 백엔드 API 실행 (선택사항)

```bash
cd personal-assistant-backend
npm install
npm run dev
```

### 5. 대시보드 실행 (선택사항)

```bash
cd personal-assistant-dashboard
npm install
npm run dev
```

## 🐳 Docker로 전체 실행

```bash
# 모든 서비스 실행
docker-compose up -d

# 서비스 확인
docker-compose ps
```

| 서비스 | 포트 | 설명 |
|--------|------|------|
| PostgreSQL | 5432 | 데이터베이스 |
| Backend API | 3001 | REST API 서버 |
| Dashboard | 5173 | 웹 대시보드 |

## 📖 MCP 도구 목록

### 메모 (Memo)
| 도구 | 설명 |
|------|------|
| `create_memo` | 새 메모 생성 |
| `list_memos` | 메모 목록 조회 |
| `get_memo` | 특정 메모 조회 |
| `update_memo` | 메모 수정 |
| `delete_memo` | 메모 삭제 |

### 할 일 (Todo)
| 도구 | 설명 |
|------|------|
| `create_todo` | 새 할 일 생성 |
| `list_todos` | 할 일 목록 조회 |
| `get_todo` | 특정 할 일 조회 |
| `update_todo` | 할 일 수정 (완료 처리 포함) |
| `delete_todo` | 할 일 삭제 |

### 일정 (Schedule)
| 도구 | 설명 |
|------|------|
| `create_schedule` | 새 일정 생성 |
| `list_schedules` | 일정 목록 조회 |
| `get_schedule` | 특정 일정 조회 |
| `update_schedule` | 일정 수정 |
| `delete_schedule` | 일정 삭제 |

### 북마크 (Bookmark)
| 도구 | 설명 |
|------|------|
| `create_bookmark` | 새 북마크 생성 |
| `list_bookmarks` | 북마크 목록 조회 |
| `get_bookmark` | 특정 북마크 조회 |
| `update_bookmark` | 북마크 수정 |
| `delete_bookmark` | 북마크 삭제 |

### 유틸리티
| 도구 | 설명 |
|------|------|
| `search_all` | 모든 데이터 통합 검색 |
| `get_today_summary` | 오늘의 요약 정보 |

## 🏗️ 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                      사용자                                 │
│         ↓                              ↓                   │
│    Cursor IDE                    Web Browser               │
│         ↓                              ↓                   │
│  ┌─────────────┐              ┌─────────────────┐          │
│  │ MCP Server  │              │ Vue Dashboard   │          │
│  │ (stdio)     │              │ :5173           │          │
│  └──────┬──────┘              └────────┬────────┘          │
│         │                              │                    │
│         │                     ┌────────▼────────┐          │
│         │                     │ REST API Server │          │
│         │                     │ :3001           │          │
│         │                     └────────┬────────┘          │
│         │                              │                    │
│         └──────────────┬───────────────┘                   │
│                        ▼                                    │
│               ┌─────────────────┐                          │
│               │   PostgreSQL    │                          │
│               │   :5432         │                          │
│               └─────────────────┘                          │
└─────────────────────────────────────────────────────────────┘
```

## 📝 환경 변수

### PostgreSQL
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=personal_assistant
DB_USER=assistant
DB_PASSWORD=assistant123
```

### Backend API
```env
API_PORT=3001
```

## 🤝 MCP란?

**MCP (Model Context Protocol)**는 AI 모델이 외부 도구와 데이터에 접근할 수 있게 해주는 프로토콜입니다.

- **서버**: 도구(Tools)와 리소스(Resources)를 정의
- **클라이언트**: Cursor IDE 등 AI 어시스턴트
- **통신**: JSON-RPC 2.0 over stdio

자세한 내용은 [MCP 공식 문서](https://modelcontextprotocol.io/)를 참조하세요.

## 📄 라이선스

MIT License

